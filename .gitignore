# Ignore workspace-local session histories so they aren't committed to the public repo
.pi/sessions/

# Ignore custom python environment if any
__pycache__/
*.pyc

# Ignore secret files if accidentally placed in the workspace
.env
*.json
!settings.json
